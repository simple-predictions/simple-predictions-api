on: push
jobs: 
  codecov-tests:
    name: Run Codecov test suite
    runs-on: ubuntu-latest
    working-directory: ../
    steps:
    - uses: actions/setup-node@v2
      with:
        node-version: '12'
    - run: ls -la
    - run: npm install